package ge.bestline.delivery.ws.repositories;

import ge.bestline.delivery.ws.entities.VolumeWeightIndex;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VolumeWeightIndexRepository extends JpaRepository<VolumeWeightIndex, Integer> {
}
